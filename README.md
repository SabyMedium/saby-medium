```markdown
# Saby Médium

Dépôt initial pour le projet Saby Médium — backend et frontend.

Étapes suivantes :
- Ajouter le code backend (API Node/Express + Prisma) ou utiliser le dépôt pour déployer sur Render.
- Ajouter le frontend (PWA / site) et déployer sur Netlify.
- Configurer Stripe (clé test) et webhook vers l'API.

Contact : SabyMedium
```
